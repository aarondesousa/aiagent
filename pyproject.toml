[project]
name = "aiagent"
version = "0.1.0"
description = "A stateful AI agent built to navigate a codebase and execute fixes."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "google-genai==1.12.1",
    "python-dotenv==1.1.0",
]
